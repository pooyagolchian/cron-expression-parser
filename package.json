{
	"name": "cron-expression-parser",
	"version": "1.0.0",
	"description": "A TypeScript package for parsing Cron schedule expressions",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"scripts": {
		"build": "tsc",
		"test": "jest",
		"test:watch": "jest --watch",
		"test:coverage": "jest --coverage",
		"dev": "tsc --watch",
		"example": "npm run build && node dist/example.js",
		"prepublishOnly": "npm run build && npm test"
	},
	"keywords": ["cron", "parser", "schedule", "expression", "crontab"],
	"author": "",
	"license": "MIT",
	"devDependencies": {
		"@types/jest": "^29.5.5",
		"@types/node": "^20.8.0",
		"jest": "^29.7.0",
		"ts-jest": "^29.1.1",
		"typescript": "^5.2.2"
	},
	"files": ["dist", "README.md", "LICENSE"],
	"engines": {
		"node": ">=14.0.0"
	}
}
